spring:
  application:
    name: config-client
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server-git

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

info:
  component: Config Client

